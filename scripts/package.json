{
  "name": "sideline-scripts",
  "version": "1.0.0",
  "description": "Admin scripts for Sideline — run outside the app",
  "private": true,
  "engines": { "node": ">=18" },
  "scripts": {
    "seed": "node seedPlayers.js",
    "seed:dry": "node seedPlayers.js --dry-run",
    "seed:epl": "node seedPlayers.js --league 39",
    "seed:ucl": "node seedPlayers.js --league 2",
    "seed:wc": "node seedFantasyWorldCup.js"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0"
  }
}
